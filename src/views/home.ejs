<%- include('partials/header'); %>
    <%- include('partials/navbar') %>
        <div class="d-flex justify-content-center">
            <img src="assets/images/home-banner.jpg">
        </div>

        <div class="d-flex justify-content-evenly">
            <button type="button" class="btn btn-info w-25 fs-4">Buscar guarderías</button>
            <button type="button" class="btn btn-warning w-25 fs-4">Publicar servicios</button>
            <% if (session.loggedin) { %>
                <a type="button" class="btn btn-primary float-end" href="logout">Logout</a>
                <%}%>
        </div>

        </body>

        </html>