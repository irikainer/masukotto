<nav class="navbar navbar-expand-lg navbar-light bg-secondary w-100 m-0 px-3">
    <div class="container-fluid">
        <a class="navbar-brand mb-0 h1" href="/">Masukotto</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse d-flex flex-row-reverse" id="navbarNav">
            <div class="navbar-nav">
                <a class="nav-link fw-semibold" href="/">Home</a>
                <a class="nav-link fw-semibold" href="#">¿Quiénes somos?</a>
                <a class="nav-link fw-semibold" href="#">Contacto</a>
                <% if (session.typeUser===1) { %>
                    <button type="button" class="btn btn-info fw-semibold">
                        <a class="text-decoration-none text-reset" href="/users">Usuarios</a></button>
                    <% }%>
                        <% if (session.loggedin) { %>
                            <button type="button" class="btn btn-warning fw-semibold">
                                <a href="/profile/<%= session.userId %>" class="text-decoration-none text-reset">Mi
                                    perfil</a></button>
                            <button type="button" class="btn btn-danger fw-semibold">
                                <a type="button" class="text-decoration-none text-reset" href="/logout">Desconectar</a>
                            </button>
                            <%} else { %>
                                <button type="button" class="btn btn-warning fw-semibold">
                                    <a href="/inicioSesion" class="text-decoration-none text-reset">Iniciar
                                        sesión</a></button>
                                <% }%>

            </div>
        </div>
    </div>
</nav>